# hc-caseta — Lutron Caseta Pro Bridge plugin for HomeCore
#
# Connects to a Caseta Smart Bridge Pro (L-BDGPRO2-WH) via the Lutron
# Integration Protocol (LIP) over telnet.  The standard Caseta bridge
# does NOT support telnet integration — only the Pro model does.

[homecore]
broker_host = "127.0.0.1"
broker_port = 1883
plugin_id   = "plugin.caseta"
password    = ""

[caseta]
host     = "10.0.10.85"       # Caseta Pro bridge IP
port     = 23                   # Telnet port (fixed, cannot change)
username = "lutron"             # Factory default
password = "integration"        # Factory default

# Optional: global fade time for dimmers (seconds).
# Individual devices can override with fade_secs.
# default_fade_secs = 1.0

# Optional: delay between reconnection attempts (seconds).
# reconnect_delay_secs = 5

# ── Logging ──────────────────────────────────────────────────────────────────

# [logging]
# dir = "logs"
# max_bytes = 10485760       # 10 MB per file
# max_files = 5
# compress = true
# log_forward_level = "info" # Forward logs to HomeCore via MQTT

# ── Devices ──────────────────────────────────────────────────────────────────
#
# Each device needs an integration_id from the Caseta Pro bridge.
# Find IDs in the Lutron app or at http://{bridge_ip}/DbXmlInfo.xml
#
# Supported kinds: dimmer, switch, shade, fan_control, pico, occupancy_sensor

[[devices]]
integration_id = 2
name           = "Holiday Lights Plug 1"
kind           = "switch"
area           = "Living Room"

[[devices]]
integration_id = 3
name           = "Holiday Lights Plug 2"
kind           = "switch"
area           = "Living Room"

[[devices]]
integration_id = 4
name           = "Holiday Lights Plug 3"
kind           = "switch"
area           = "Living Room"

[[devices]]
integration_id = 7
name           = "Holiday Lights Plug 4"
kind           = "switch"
area           = "Living Room"

[[devices]]
integration_id = 5
name           = "Holiday Lights Plug 5"
kind           = "switch"
area           = "Hallway"

[[devices]]
integration_id = 8
name           = "Holiday Lights Plug 6"
kind           = "switch"
area           = "Family Room"

[[devices]]
integration_id = 10
name           = "Outdoor Holiday Lights Plug 1"
kind           = "switch"
area           = "Outside"

[[devices]]
integration_id = 6
name           = "Living Room Pico"
kind           = "pico"
area           = "living_room"
buttons        = [2, 3, 4, 5, 6]   # Button component numbers

[[devices]]
integration_id = 9
name           = "Holiday Lights Pico"
kind           = "pico"
area           = "living_room"
buttons        = [2, 3, 4, 5, 6]   # Button component numbers

# [[devices]]
# integration_id = 10
# name           = "Bedroom Shade"
# kind           = "shade"
# area           = "bedroom"
# invert_position = false     # false = Lutron native (0=open)

# [[devices]]
# integration_id = 8
# name           = "Bedroom Fan"
# kind           = "fan_control"
# area           = "bedroom"

# [[devices]]
# integration_id = 15
# name           = "Hallway Motion"
# kind           = "occupancy_sensor"
# area           = "hallway"
